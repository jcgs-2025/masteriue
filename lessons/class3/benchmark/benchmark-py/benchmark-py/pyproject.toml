[project]
name = "benchmark-py"
version = "0.1.0"
description = ""
authors = [
    {name = "Your Name",email = "jcguerrero@correo.iue.edu.co"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest (>=8.3.4,<9.0.0)"
]

[tool.poetry]
packages = [{include = "benchmark_py", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
