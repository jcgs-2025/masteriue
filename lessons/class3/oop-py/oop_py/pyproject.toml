[project]
name = "oop-py"
version = "0.1.0"
description = ""
authors = [
    {name = "Your Name",email = "jcguerrero@correo.iue.edu.co"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry.scripts]
main = "main:main"

[tool.poetry]
packages = [{include = "oop_py", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
