[project]
name = "euler-py"
version = "0.1.0"
description = ""
authors = [
    {name = "Your Name",email = "jcguerrero@correo.iue.edu.co"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "maturin (>=1.8.3,<2.0.0)",
    "numpy (>=2.2.4,<3.0.0)",
    "matplotlib (>=3.10.1,<4.0.0)",
    "pytest (>=8.3.5,<9.0.0)",
    "pytest-benchmark (>=5.1.0,<6.0.0)"
]

[tool.poetry]
packages = [{include = "euler_py", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
